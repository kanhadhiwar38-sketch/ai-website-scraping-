{
  "name": "webrecon-ai",
  "private": true,
  "version": "0.1.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev:web": "pnpm --filter @webrecon/web dev",
    "dev:api": "pnpm --filter @webrecon/api dev",
    "typecheck": "pnpm -r --if-present run typecheck",
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm -r --if-present run test",
    "build": "pnpm -r --if-present run build"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
